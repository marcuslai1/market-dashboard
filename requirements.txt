# <1.59: Streamlit 1.59 changed st.radio's internal DOM (input in a span, circle
# beside the label text), which breaks theme.css's nav-tab circle-hide selectors.
# Lift the cap only together with the theme.css selector update + baseline regen.
streamlit>=1.39.0,<1.59
pandas>=2.0.0,<3
plotly>=5.18.0,<7
yfinance>=0.2.40,<2
