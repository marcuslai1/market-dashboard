# Baseline screenshots are canonical binary artifacts generated in the pinned
# Playwright image (see tests/visual/README.md). Treat them as binary so git
# never line-ending-normalizes or text-diffs them.
tests/visual/baselines/*.png binary
